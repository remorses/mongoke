
API Reference
=============

Table of contents

Available APIs
--------------

  -------------------------------------------------------------------------------------------
  API               Endpoint                                                Access
  ----------------- ------------------------------------------------------- -----------------
  GraphQL           `/v1/graphql <graphql_api>`{.interpreted-text           Permission rules
                    role="ref"}                                             

  Legacy GraphQL    `/v1alpha1/graphql <graphql_api>`{.interpreted-text     Permission rules
                    role="ref"}                                             

  Schema/Metadata   `/v1/query <schema_metadata_api>`{.interpreted-text     Admin only
                    role="ref"}                                             

  Version           `/v1/version <version_api>`{.interpreted-text           Public
                    role="ref"}                                             

  Health            `/healthz <health_api>`{.interpreted-text role="ref"}   Public

  PG Dump           `/v1alpha1/pg_dump <pg_dump_api>`{.interpreted-text     Admin only
                    role="ref"}                                             

  Config            `/v1alpha1/config <config_api>`{.interpreted-text       Admin only
                    role="ref"}                                             

  Explain           `/v1/graphql/explain <explain_api>`{.interpreted-text   Admin only
                    role="ref"}                                             
  -------------------------------------------------------------------------------------------

### GraphQL API

All GraphQL requests for queries, subscriptions and mutations are made
to the GraphQL API.

See details at `api_reference_graphql`{.interpreted-text role="ref"}.

### Schema / metadata API

Hasura exposes a schema / metadata API for managing metadata for
permissions/relationships or for directly executing SQL on the
underlying Postgres.

This is primarily intended to be used as an `admin` API to manage the
Hasura schema and metadata.

See details at `metadata_apis`{.interpreted-text role="ref"}.

### Version API

The `/v1/version` is a public endpoint that responds with the current
server version in JSON format.

See details at `version_api_reference`{.interpreted-text role="ref"}.

### Health check API

The `/healthz` is a public endpoint that returns the server health
status.

See details at `health_api_reference`{.interpreted-text role="ref"}.

### pg\_dump API

The `/v1alpha1/pg_dump` is an admin-only endpoint that can be used to
execute `pg_dump` on the Postgres instance connected to Hasura. The
`pg_dump` CLI tool\'s argument can be passed as a POST request body to
the API and the response is sent back to the client.

See details at `pg_dump_api_reference`{.interpreted-text role="ref"}.

### Config API

`v1alpha1/config` is an admin-only endpoint to get the current server
configuration.

See details at `config_api_reference`{.interpreted-text role="ref"}.

### Explain API

`v1/graphql/explain` returns the Postgres plan for a query or
subscription based on the defined permissions.

See details at `explain_api_reference`{.interpreted-text role="ref"}.

Supported PostgreSQL types
--------------------------

You can refer to the following to know about all PostgreSQL types
supported by the Hasura GraphQL engine:

-   `Supported PostgreSQL types <api_postgres_types>`{.interpreted-text
    role="ref"}

GraphQL API \<graphql-api/index\> Schema / Metadata APIs
\<schema-metadata-api/index\> Version API \<version\> Health check API
\<health\> PG Dump API \<pgdump\> Config API \<config\> Explain API
\<explain\> Supported PostgreSQL types \<postgresql-types\>
