
Hasura CLI: hasura metadata
===========================

Manage Hasura GraphQL engine metadata saved in the database.

Synopsis
--------

Manage Hasura GraphQL engine metadata saved in the database.

Alias: md

Options
-------

    --admin-secret string            admin secret for Hasura GraphQL engine 
    --certificate-authority string   path to a cert file for the certificate authority
    --endpoint string                http(s) endpoint for Hasura GraphQL Engine

> -h, \--help help for metadata
>
> :   \--insecure-skip-tls-verify skip TLS verification and disable cert
>     checking (default: false)
>
Options inherited from parent commands
--------------------------------------

    --envfile string      .env filename to load ENV vars from (default ".env")
    --log-level string    log level (DEBUG, INFO, WARN, ERROR, FATAL) (default "INFO")
    --no-color            do not colorize output (default: false)
    --project string      directory where commands are executed (default: current dir)
    --skip-update-check   skip automatic update check on command execution

SEE ALSO
--------

-   `hasura <hasura>`{.interpreted-text role="ref"} - Hasura GraphQL
    engine command line tool
-   `hasura metadata apply <hasura_metadata_apply>`{.interpreted-text
    role="ref"} - Apply Hasura metadata on a database
-   `hasura metadata clear <hasura_metadata_clear>`{.interpreted-text
    role="ref"} - Clear Hasura GraphQL engine metadata on the database
-   `hasura metadata diff <hasura_metadata_diff>`{.interpreted-text
    role="ref"} - (PREVIEW) Show a highlighted diff of Hasura metadata
-   `hasura metadata export <hasura_metadata_export>`{.interpreted-text
    role="ref"} - Export Hasura GraphQL engine metadata from the
    database
-   `hasura metadata inconsistency <hasura_metadata_inconsistency>`{.interpreted-text
    role="ref"} - Manage inconsistent objects in Hasura Metadata
-   `hasura metadata reload <hasura_metadata_reload>`{.interpreted-text
    role="ref"} - Reload Hasura GraphQL engine metadata on the database

*Auto generated by spf13/cobra*
