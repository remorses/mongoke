
bash

Hasura CLI
==========

Table of contents

The `Hasura CLI` is a command line tool which is the primary mode of
managing Hasura projects and migrations.

Installation
------------

Refer to `install_hasura_cli`{.interpreted-text role="ref"}.

Configuration
-------------

Refer to `cli_config_reference`{.interpreted-text role="ref"}.

Commands
--------

-   `hasura actions <hasura_actions>`{.interpreted-text role="ref"}
-   `hasura <hasura>`{.interpreted-text role="ref"}
-   `hasura actions codegen <hasura_actions_codegen>`{.interpreted-text
    role="ref"}
-   `hasura actions create <hasura_actions_create>`{.interpreted-text
    role="ref"}
-   `hasura actions use-codegen <hasura_actions_use-codegen>`{.interpreted-text
    role="ref"}
-   `hasura completion <hasura_completion>`{.interpreted-text
    role="ref"}
-   `hasura console <hasura_console>`{.interpreted-text role="ref"}
-   `hasura init <hasura_init>`{.interpreted-text role="ref"}
-   `hasura metadata <hasura_metadata>`{.interpreted-text role="ref"}
-   `hasura metadata apply <hasura_metadata_apply>`{.interpreted-text
    role="ref"}
-   `hasura metadata clear <hasura_metadata_clear>`{.interpreted-text
    role="ref"}
-   `hasura metadata diff <hasura_metadata_diff>`{.interpreted-text
    role="ref"}
-   `hasura metadata export <hasura_metadata_export>`{.interpreted-text
    role="ref"}
-   `hasura metadata inconsistency <hasura_metadata_inconsistency>`{.interpreted-text
    role="ref"}
-   `hasura metadata inconsistency drop <hasura_metadata_inconsistency_drop>`{.interpreted-text
    role="ref"}
-   `hasura metadata inconsistency list <hasura_metadata_inconsistency_list>`{.interpreted-text
    role="ref"}
-   `hasura metadata inconsistency status <hasura_metadata_inconsistency_status>`{.interpreted-text
    role="ref"}
-   `hasura metadata reload <hasura_metadata_reload>`{.interpreted-text
    role="ref"}
-   `hasura migrate <hasura_migrate>`{.interpreted-text role="ref"}
-   `hasura migrate apply <hasura_migrate_apply>`{.interpreted-text
    role="ref"}
-   `hasura migrate create <hasura_migrate_create>`{.interpreted-text
    role="ref"}
-   `hasura migrate squash <hasura_migrate_squash>`{.interpreted-text
    role="ref"}
-   `hasura migrate status <hasura_migrate_status>`{.interpreted-text
    role="ref"}
-   `hasura plugins <hasura_plugins>`{.interpreted-text role="ref"}
-   `hasura plugins install <hasura_plugins_install>`{.interpreted-text
    role="ref"}
-   `hasura plugins list <hasura_plugins_list>`{.interpreted-text
    role="ref"}
-   `hasura plugins uninstall <hasura_plugins_uninstall>`{.interpreted-text
    role="ref"}
-   `hasura plugins upgrade <hasura_plugins_upgrade>`{.interpreted-text
    role="ref"}
-   `hasura scripts <hasura_scripts>`{.interpreted-text role="ref"}
-   `hasura scripts update-project-v2 <hasura_scripts_update-project-v2>`{.interpreted-text
    role="ref"}
-   `hasura update-cli <hasura_update-cli>`{.interpreted-text
    role="ref"}
-   `hasura version <hasura_version>`{.interpreted-text role="ref"}

Uninstalling
------------

Refer to `uninstall_hasura_cli`{.interpreted-text role="ref"}.

install-hasura-cli Configuration reference \<config-reference\> hasura
\<hasura\> hasura actions \<hasura\_actions\> hasura actions codegen
\<hasura\_actions\_codegen\> hasura actions create
\<hasura\_actions\_create\> hasura actions use-codegen
\<hasura\_actions\_use-codegen\> hasura completion
\<hasura\_completion\> hasura console \<hasura\_console\> hasura init
\<hasura\_init\> hasura metadata \<hasura\_metadata\> hasura metadata
apply \<hasura\_metadata\_apply\> hasura metadata clear
\<hasura\_metadata\_clear\> hasura metadata diff
\<hasura\_metadata\_diff\> hasura metadata export
\<hasura\_metadata\_export\> hasura metadata inconsistency
\<hasura\_metadata\_inconsistency\> hasura metadata inconsistency drop
\<hasura\_metadata\_inconsistency\_drop\> hasura metadata inconsistency
list \<hasura\_metadata\_inconsistency\_list\> hasura metadata
inconsistency status \<hasura\_metadata\_inconsistency\_status\> hasura
metadata reload \<hasura\_metadata\_reload\> hasura migrate
\<hasura\_migrate\> hasura migrate apply \<hasura\_migrate\_apply\>
hasura migrate create \<hasura\_migrate\_create\> hasura migrate squash
\<hasura\_migrate\_squash\> hasura migrate status
\<hasura\_migrate\_status\> hasura plugins \<hasura\_plugins\> hasura
plugins install \<hasura\_plugins\_install\> hasura plugins list
\<hasura\_plugins\_list\> hasura plugins uninstall
\<hasura\_plugins\_uninstall\> hasura plugins upgrade
\<hasura\_plugins\_upgrade\> hasura scripts \<hasura\_scripts\> hasura
scripts update-project-v2 \<hasura\_scripts\_update-project-v2\> hasura
update-cli \<hasura\_update-cli\> hasura version \<hasura\_version\>
uninstall-hasura-cli
